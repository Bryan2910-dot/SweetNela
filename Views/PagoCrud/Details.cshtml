@model SweetNela.Models.Pago

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Pago</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.User.UserName) <!-- Cambiado a User.UserName -->
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.User.UserName) <!-- Cambiado a User.UserName -->
        </dd>
    </dl>
</div>
<div>|
    <a asp-action="Index">Back to List</a>
</div>
